# 🚀 论文助手 — 部署说明

> 5分钟部署，零成本上线，永久免费

---

## 方式一：GitHub Pages（推荐）

### 第1步：上传代码
1. 注册/登录 [GitHub](https://github.com)
2. 点击右上角 **+** → **New repository**
3. Repository name 填写 `thesis-helper`（随便写）
4. 勾选 **Public**（公开，免费）
5. 点击 **Create repository**
6. 点击 **uploading an existing file** 链接
7. 把源码文件夹里所有文件拖进去
8. 点击 **Commit changes**

### 第2步：开启Pages
1. 进入仓库 → 点 **Settings** 标签
2. 左侧菜单点 **Pages**
3. **Branch** 选 `main`，文件夹选 `/ (root)`
4. 点 **Save**
5. 等1-2分钟，页面刷新后会显示：
   ```
   Your site is live at https://你的用户名.github.io/thesis-helper/
   ```

### 第3步：访问
- 打开上面那个网址
- 手机上也能打开，Safari/Chrome可以「添加到主屏幕」当App用

---

## 方式二：Vercel（如果嫌GitHub麻烦）

1. 注册 [vercel.com](https://vercel.com)（用GitHub一键登录）
2. 点 **New Project**
3. 上传源码文件夹
4. 点 **Deploy**，等30秒
5. 获得网址：`https://项目名.vercel.app`

---

## 本地运行（不用部署）

如果不想部署，直接用浏览器打开 `index.html` 文件也可以使用。

---

## 自定义

### 改标题/logo
打开 `index.html`，搜索「论文助手」替换成你的名字

### 改颜色
打开 `css/style.css`，搜索 `--gold: #c9a96e` 改成你喜欢的颜色

### 添加功能
JS模块在 `js/` 文件夹，每个工具是独立文件，修改方便

---

## 需要帮助？

- GitHub部署详细图文教程：搜索「GitHub Pages 部署教程」
- 想让网站更好看？可以改 `css/style.css` 里的颜色变量
- 想添加新功能？在 `js/` 文件夹新建一个模块，参考现有代码结构

---

## 文件说明

```
thesis-helper/
├── index.html          # 主页面
├── css/style.css       # 样式文件
├── js/
│   ├── reference.js    # 参考文献格式化
│   ├── checklist.js    # 格式检查清单
│   ├── timeline.js     # 进度管理
│   ├── acknowledge.js  # 致谢生成
│   ├── defense.js      # 答辩准备
│   └── app.js          # 主控制器
├── assets/             # 图标等资源
├── manifest.json       # PWA配置
└── sw.js               # 离线缓存
```
